{
    "rewrites": [
        {
            "destination": "/",
            "source": "/(.*)"
        }
    ],
    "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "installCommand": "npm install",
  "buildCommand": "npm install && npm run build"
}